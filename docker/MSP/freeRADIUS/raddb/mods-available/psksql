sql psksql {
        driver = "rlm_sql_sqlite"
        sqlite {
                filename = "/opt/SPs/radiussql.db"
                bootstrap = "${modconfdir}/${..:name}/main/sqlite/radiussql.sql"
        }
}
